// Single source of truth for the app version, shown next to the wordmark in the
// header. Bump on each release so every device can confirm at a glance which
// build it's running — useful when a sync/behaviour fix must reach all devices.
export const APP_VERSION = "0.1.0";
